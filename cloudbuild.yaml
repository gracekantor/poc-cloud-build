steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - functions
  - deploy
  - function-2
  - --runtime python39
  - --source=.
  - --trigger-bucket 
  - homepage
  
options:
      logging: CLOUD_LOGGING_ONLY
