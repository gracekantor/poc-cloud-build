steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - hello_get
  - --region=us-central1
  - --source=.
  - --trigger-http
  - --runtime=python39
  
options:
      logging: CLOUD_LOGGING_ONLY
