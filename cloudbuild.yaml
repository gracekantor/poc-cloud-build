steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - functions
  - deploy
  - function-2
  - --source=.
  - --trigger-bucket 
  - --runtime python39
  - homepage
  
options:
      logging: CLOUD_LOGGING_ONLY
